# yt-dlp docker wrapper

simple wrapper that runs a given version of the [yt-dlp](https://github.com/yt-dlp/yt-dlp) inside the docker container.
this guarantees a better isolation, while still keeping convenience of running the latest version.
